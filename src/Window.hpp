#ifndef WINDOW_HPP
#define WINDOW_HPP

#include <unordered_map>

class Window 
{
    public:
        Window() = default;
        ~Window() = default;

        bool init();

    private:
        
};

#endif // !WINDOW_HPP
